# 无界便签看板产品需求规格说明书 (PRD)

## 1. 操作与功能规格清单

### 1.1 画布与视窗操作
| 用户操作 | 系统响应与动作 |
| :--- | :--- |
| **空白处鼠标右键拖拽** | 平移漫游画布，二维坐标系无边界限制。与卡片右键操作严格隔离。 |
| **`Space + 鼠标左键` 拖拽** / **`Alt + 鼠标左键` 拖拽** | 平移漫游画布。 |
| **鼠标滚轮 上滚 / 下滚** | 以当前鼠标光标所在坐标为中心，进行 0.2x ~ 3.0x 无级等比缩放。未选中的便签内部滚动穿透至画布，不干扰视窗缩放。 |
| **双击 单张卡片** | 视窗平滑居中并将该卡片放大至视窗 80% 大小；再次双击恢复原视野。 |
| **双击 空白画布** | **在当前鼠标光标位置就地新建一张纯文本便签**，并自动设为高亮选中。顶部悬浮栏另提供“自适应全览”按钮。 |
| **点击左上角“+ 新建便签”** | 在当前视窗中心位置生成一张纯文本便签，并自动高亮选中。 |
| **空白处按住鼠标左键 拖拽** | 拉出矩形虚线选框多选卡片；按住 `Shift` 可追加或减选。 |
| **按住鼠标中键拖拽** | **平移漫游画布**（无论光标起始于空白画布或便签卡片上方），拖动期间光标全程变为抓手手势（`cursor-grabbing`），松开后恢复默认手势；同时屏蔽系统默认的自动滚动圆圈。 |

---

### 1.2 粘贴摄入、卡片生成与复制粘贴操作
在画布任意位置按下 **`Ctrl + V`**，卡片中心自动对齐当前鼠标所在坐标，根据剪贴板内容类型执行对应处理：

| 剪贴板输入 / 操作 | 系统识别与卡片生成规则 |
| :--- | :--- |
| **卡片复制与粘贴 (`Ctrl + C` / `Ctrl + V` / `Ctrl + D`)** | 1. **`Ctrl + C` 复制卡片**：选中一张或多张便签（焦点不在文本框内）按下，复制卡片数据至内存与剪贴板；<br>2. **`Ctrl + V` 粘贴卡片**：若剪贴板包含复制便签数据，在当前鼠标光标世界坐标粘贴生成克隆便签组（保持多张卡片的相对间距），自动分配新 ID、更新层级并选中新便签；<br>3. **`Ctrl + D` 快速复制**：原地微量偏移直接克隆生成当前选中的便签。 |
| **图片**（截图 / 复制图片） | 1. 在光标位置生成图片卡片，按原图比例展示，所有边角均为**直角设计**（`rounded-none`）；<br>2. **首次粘贴仅展示原图**，保持原图直角比例呈现，不自动覆盖或遮挡文本。 |
| **网址链接**（URL 文本） | 1. 自动请求目标网址元数据，针对飞书（Feishu）、Bilibili 空间及 Instagram 帖子/Reel 等特殊页面专属适配；<br>2. 优先提取网页头图（`og:image`）；**若网站无头图，自动调用无头浏览器生成首页首屏真实截图作为卡片封面**；<br>3. **维持头图原始比例并自适应卡片高度**：严格保持头图的自然长宽比（`naturalWidth / naturalHeight`），根据头图的宽高比自动调整卡片总高度（`图片等比高度 + 底部信息栏固定高度`），卡片封面采用 `object-contain` 完整渲染，彻底消除上下/左右被截断裁剪的问题；无头图卡片自动收缩为极简紧凑高度；<br>4. 提取核心标题（`og:title` / `<title>`）和完整描述（`og:description`）；<br>5. **严格限制跳转触发区域**：**必须点击底部的具体网址链接才能进入网站**，点击上方封面图片或卡片其他空白区域仅用于选择/拖拽，**绝不触发跳转**；<br>6. **必须在已高亮状态下点击链接方可跳转**：首次单击未高亮的卡片仅执行选中高亮，即使点击到底部链接也不会误跳；仅在卡片已高亮前提下再次点击链接，才在新标签页中打开；<br>7. **双击网页标题可直接就地编辑修改**（需在卡片高亮状态下）；<br>8. **鼠标悬停在卡片上 >300ms 时，浮出气泡显示完整描述**（仅在高亮卡片上响应），移开浮层消失。 |
| **纯文本**（普通文字） | 1. 在光标位置生成直角文本卡片；<br>2. 保持原文字换行与段落格式；<br>3. 单击高亮后再次点击就地编辑文字，失去焦点或按 ESC 自动保存。 |

---

### 1.3 卡片直接操作与交互层级规范（高亮后方可编辑）
| 用户操作 / 交互层级 | 系统响应与动作 |
| :--- | :--- |
| **交互层级：未高亮状态 (`!isSelected`)** | 1. **防滚动与防缩放冲突**：便签内部文本容器处于穿透保护状态（`pointer-events-none`），隐藏滚动条；鼠标悬停时不触发内容交互，鼠标滚轮 100% 作用于全局画布缩放；<br>2. **防误触跳转与编辑**：单击未选中的卡片仅执行“选中并高亮卡片”，绝不触发网页链接跳转，不进入文字编辑；双击标题不响应。 |
| **交互层级：已高亮状态 (`isSelected`)** | 1. 卡片呈现蓝色高亮线框（`ring-2 ring-blue-500`）；<br>2. **文字编辑**：点击文本内容进入编辑模式（`<textarea>`），光标自动就位；**编辑文字时全局禁用画布滚轮缩放**，滚轮仅用于滚动便签内长文本，彻底杜绝文字编辑与画布缩放冲突；失焦或取消选中自动保存并退回安全展示态；<br>3. **网页跳转**：点击卡片底部的独立链接按钮（`text-blue-400`）在新标签页中打开目标链接（点击上方图片依然仅为选择/拖拽）；双击标题可就地修改标题。 |
| **鼠标左键拖拽卡片** | 100% 毫秒级跟手移动单张或选中的多张卡片，不发生画面黏滞或延迟。 |
| **卡片四周与四角调节手柄拖拽** | 卡片提供上、下、左、右及四角共 8 个调节手柄（直角风格），支持自由拉伸调整卡片的宽度与高度。 |
| **按住 `Ctrl + Alt + 左键` 向左/向右滑动** | 等比缩放卡片尺寸：向右滑动放大，向左滑动缩小。 |
| **按下 `Ctrl + N`** | 在当前鼠标光标位置新建一张空白纯文本卡片。 |
| **按下 `Delete` 或 `Backspace`** | 删除当前选中的卡片或分组。 |
| **按下 `Ctrl + Z`** | 撤销上一步删除、移动、缩放、复制等操作。 |

---

### 1.4 卡片右键饼状菜单 (Pie Menu) 与标题/日期标记
为所有卡片快速赋予标题与时间属性，支持直觉滑动手势与精确交互：

| 交互维度 | 规格定义与系统行为 |
| :--- | :--- |
| **呼出方式** | **在任意卡片上按住鼠标右键并拖动**（或右键点击），以光标起始点为中心呼出半透明磨砂**三扇区圆形饼状菜单 (Radial Pie Menu)**。<br>*注：空白画布右键拖动严格保持平移漫游，两者互不干扰。* |
| **上扇区：`设置标题`** | 1. **手势操作**：按住右键向上方滑动松开，立即呼出便签标题编辑浮层，输入框自动聚焦并全选现有标题；<br>2. **点击操作**：右键呼出菜单后，单击上方 `设置标题` 扇区进入输入模式；<br>3. **全卡片支持**：**适用于所有便签类型**（纯文本卡片、图片卡片、网页卡片）；<br>4. **保存与清除**：输入后按 `Enter` 键确认保存；若卡片已有标题，提供“清除标题”快捷按钮；按 `ESC` 随时退出。 |
| **左扇区：`NOW`** | 1. **手势操作**：按住右键向左侧滑动松开，直接将当前此刻日期与精确时间（如 `2026-09-15 00:08`）标记至卡片；<br>2. **点击操作**：右键呼出菜单后，单击左侧 `NOW` 扇区即刻生效；<br>3. 卡片右上角实时浮现绿色磨砂时间徽标。 |
| **右扇区：`输入时间`** | 1. **手势操作**：按住右键向右侧滑动松开，立即展开智能时间输入卡片，输入框自动聚焦；<br>2. **点击操作**：右键呼出菜单后，单击右侧 `输入时间` 扇区进入输入模式。 |
| **中心 Hub / 取消** | 1. 拖动过程中中心 Hub 显示动态方向引导点；<br>2. 若卡片已存在日期，中心 Hub 提供“清除”按钮可一键移除日期；<br>3. 点击空白背景或按 `ESC` 随时退出菜单。 |
| **智能日期识别算法规则** | 1. **年份缺省自动补齐**：用户只需输入月日（如 `06/26`、`6/26`、`6-26`、`0626`、`6月26日`），系统**默认补齐今年年份**（如当前为 2026 年，自动补齐为 `2026-06-26`）；<br>2. **多样化格式兼容**：支持 `YYYY-MM-DD`、`YYYY/MM/DD`、`YYYYMMDD`、`YY-MM-DD`、纯数字、自然语言（`今天`、`明天`、`后天`、`下周五`）及带时间后缀（如 `14:30`、`上午10点`）；<br>3. **非法日期校验拦截**：严格校验闰年与月份天数（如 `02/30`、`06/32` 等非法日期均判定为无效）。 |
| **输入框实时校验与视觉反馈** | 1. **非法 / 不完整状态**：输入内容不足以判断或格式不合法时，**输入框边框严格保持灰色**，提示文本显示灰色未识别说明，确认按钮置灰禁用，**按 Enter 回车键不可确认**；<br>2. **有效状态**：当输入足以判定确切日期时，**输入框边框自动变为翠绿色**，下方提示实时转为绿色识别预览（例如 `✓ 识别为: 2026年06月26日 (周五)`），确认按钮高亮为绿色，**允许按下 Enter 回车键或点击按钮直接保存**。 |

---

### 1.5 分组 (Group) 操作
| 用户操作 | 系统响应与动作 |
| :--- | :--- |
| **多选卡片后按下 `Ctrl + G`** | 将选中的卡片合并为一个分组，外部生成直角半透明容器边框，默认显示组名 `Group N`。 |
| **拖拽分组边框 / 标题栏** | 移动整个分组，组内所有卡片保持相对位置联动位移。 |
| **双击 分组名称** | 变为文本输入框，可直接修改组名，回车或失焦确认。 |
| **单选组内卡片并拖动** | 在组内微调单张卡片位置；拖出组外边界自动移出分组；外部卡片拖入组内边界自动加入分组。 |
| **选中分组按下 `Ctrl + Shift + G`** | 解散分组，移除外框，组内卡片保留在当前位置。 |

---

### 1.6 整理与排版操作
| 用户操作 | 系统响应与动作 |
| :--- | :--- |
| **按下 `Ctrl + P`** | 自动装箱排版：将选中的卡片（未选中则为全量卡片）以无重叠、间距相等的矩形紧凑拼合排列。 |
| **按下 `Ctrl + ↑ / ↓ / ← / →`** | 多选卡片边缘对齐：分别实现顶对齐、底对齐、左对齐、右对齐。 |
| **拖拽时按住 `Shift + Space`** | 开启磁吸：当卡片边缘距离邻近卡片小于 12px 时自动吸附对齐，并显示对齐辅助线。 |

---

## 2. 数据存储与持久化机制

系统采用 **“前端即时缓存 + 后端本地磁盘同步”的双层持久化机制**，保障断网、离线、页面刷新或服务重启时数据均不丢失。

```
[ 用户操作 (新建/编辑/位移/缩放/标记日期/打组/删除) ]
                     │
                     ▼
       ┌─────────────────────────────┐
       │ 1. 前端内存状态 (React State) │
       └──────────────┬──────────────┘
                      │ (防抖 400ms / 卸载前 Flush)
                      ▼
       ┌─────────────────────────────┐
       │ 2. 前端持久化 (LocalStorage) │ ──> 离线/无后端时保证毫秒级本地可用
       └──────────────┬──────────────┘
                      │ (静默异步 POST /api/cards)
                      ▼
       ┌─────────────────────────────┐
       │ 3. 后端持久化 (JSON 文件)    │ ──> 落盘至 backend/data/cards.json
       └─────────────────────────────┘
```

### 2.1 存储分层与触发规则
| 存储层级 | 存储介质 | 触发时机 | 机制与行为 |
| :--- | :--- | :--- | :--- |
| **前端本地缓存** | 浏览器 `LocalStorage` (键名: `pinboard_cards_v1`) | 卡片创建、内容修改、拖拽松手、缩放结束、日期标记、打组、删除等任意状态变更 | **400ms 防抖写入**；页面关闭/刷新前触发 `flushStoredCards` 立即同步写入，保障毫秒级恢复，支持纯前端离线运行。 |
| **后端持久化** | 本地磁盘文件 (`backend/data/cards.json`) | 前端 LocalStorage 写入完成后自动触发 | 前端静默发起 `POST /api/cards`，后端以 UTF-8 JSON 格式原子落盘。网络异常或单机无后端时不阻断前端正常使用。 |

### 2.2 数据初始化与加载规则
1. **页面初始化启动流程**：
   - 优先向后端发起 `GET /api/cards` 请求读取本地持久化数据；
   - 若后端接口请求成功且返回卡片数组，更新内存并同步刷新 LocalStorage；
   - 若后端不可用或处于纯离线环境，自动降级读取本地 `LocalStorage` 数据；
   - 若两者均为空，则加载初始内置引导卡片。

---

## 3. 界面与卡片样式设计规范

1. **直角工业极简美学**：
   - **全面取消卡片圆角**：卡片外框、图片展示容器、缩放手柄、菜单面板均采用直角设计（`rounded-none`）；
   - 无常驻顶部/底部臃肿工具栏；
   - 保持极客极简暗色半透明毛玻璃质感（`backdrop-blur-md`）。
2. **卡片视觉与状态指示**：
   - 不显示多余的创建时间、修改时间与字数统计；
   - **卡片标题顶栏规范**：所有便签卡片均支持设置标题。文本卡片与图片卡片拥有标题时，在卡片顶部呈现直角暗色半透明标题栏（`border-b border-white/10 text-xs font-semibold`）；网页卡片在展示区呈现网页标题，支持双击就地编辑；
   - **右上角时间指示徽标**：卡片拥有日期标记时，右上角浮动显示半透明翠绿时间徽标，直观提示提醒日期（如 `06-26`）；
   - 仅包含图片、网页缩略图+标题（悬停显描述）、纯文本三种纯粹形式。

---

## 4. 图片识别开源轻量化算法方案

* **选用算法**：`RapidOCR`（基于 PaddleOCR-v4 ONNXRuntime）。
* **技术指标**：
  - **模型大小**：约 15MB（含检测、分类、识别三个子模型）。
  - **硬件要求**：纯 CPU 运行，无需 GPU。
  - **推理耗时**：单图 30ms ~ 80ms。
  - **准确率**：中文与英文文本识别准确率 98.5%+。
* **接口规范**：
  - `POST /api/recognize-image`
  - 入参：图片二进制流（File / Blob）
  - 出参：
    ```json
    {
      "success": true,
      "title": "提取的首行文字",
      "text": "提取的完整文本内容",
      "count": 5
    }
    ```

---

## 5. 网页元数据接口与首页截图兜底规范

* **接口规范**：
  - `GET /api/fetch-metadata?url={target_url}`
* **三级调度架构与解析规则保护库**：
  - 建立专有台账文档：`docs/image_parsing_rules.md`，执行严格的**用户确认 (OK) 准入制与冻结原则**；
  - 核心调度引擎 (`backend/services/scrapers/registry.py`) 严格按优先级执行三级分发：
    1. **🔒 保护库 (`protected/`)**：用户验证通过并锁定的专属规则，**严禁未经指令擅自修改**：
       - `P-001 Bilibili`：官方公开 API，提取最高清原图 `pic`，携带 Referer 伪装；
       - `P-002 Instagram`：服务端 Embed 预渲染机制 + 纯净 UA，100% 提取高清大图，无裁切原比例呈现；
       - `P-003 YouTube`：提取标准视频 ID，官方 oEmbed 拉取标题与频道，1280x720 全高清封面；
       - `P-004 Pinterest`：自动检测本地代理，原图画质升频输出 `/originals/` 原始素材大图。
    2. **⏳ 试验库 (`experimental/`)**：新接入网站规则的孵化与调优区（如飞书文档，暂时待后续突破私密头图）；
    3. **🌐 通用兜底层 (`fallback.py`)**：标准 OpenGraph / Twitter Cards 提取，无图时自动检测代理并调用 Headless Chrome 视口截图保底（鉴权与登录页自动豁免截图）。
* **自动化回归测试守卫**：
  - 设立 `backend/tests/test_protected_scrapers.py` 测试锁，保障受保护规则永不回退。

---

## 6. 数据模型结构

```typescript
// 1. 卡片数据结构
interface Card {
  id: string;               // 唯一标识
  type: 'image' | 'web' | 'text'; // 类型
  x: number;                // X坐标
  y: number;                // Y坐标
  width: number;            // 宽度
  height: number;           // 高度
  zIndex: number;           // 层级
  groupId?: string | null;  // 所属组ID
  content?: string;         // 文本内容 / OCR全文
  title?: string;           // 网页标题 / 图片提取标题
  url?: string;             // 网页链接
  image?: string;           // 网页缩略图 / 首屏截图路径 / 图片卡片Base64或路径
  description?: string;     // 网页完整描述 (悬停展示)
  favicon?: string;         // 网站图标
  reminder?: string | null; // 标记日期与提醒时间 (如 "2026-06-26" 或 "2026-06-26 14:30")
}

// 2. 分组数据结构
interface Group {
  id: string;               // 唯一标识
  title: string;            // 组名
  x: number;                // X坐标
  y: number;                // Y坐标
  width: number;            // 宽度
  height: number;           // 高度
}
```
